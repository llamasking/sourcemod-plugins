{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile",
            "type": "shell",
            "command": "${workspaceFolder}/spcomp64 ${relativeFile} -o ${relativeFileDirname}/../plugins/${fileBasenameNoExtension}.smx",
            "problemMatcher": [
                "$msCompile"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}